<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.blog.model.LikeRepository">

  <select id="findAll" resultType="shop.mtcoding.blog.model.Like">
    select * from like_tb
  </select>

  <insert id="insert">
      insert into like_tb(user_id, board_id, likeNum) VALUES (#{userId}, #{boardId}, #{likeNum});
   </insert>
    
	<delete id="deleteById">
	  delete form like_tb where user_id = 1 and board_id = 1;
	</delete>
</mapper>